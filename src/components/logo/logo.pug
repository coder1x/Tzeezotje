mixin logo(options = {})
  -
    const {
      isColor = false,
      } = options;

    const logoRGB = require('./img/logo-rgb.webp')
    const logo = isColor ? logoRGB : require('./img/logo.webp');

    const BASE_CLASS = 'logo';

  a(class=`${BASE_CLASS}` href="/" aria-label="Home")
    picture
      source(media='(max-width: 1199px)' srcset=logoRGB)
      source(media='(min-width: 1199px)' srcset=logo)
      img(
        class=`${BASE_CLASS}__image`
        width="269" 
        height="55"
        src=logo 
        alt='Tzeezotje'
        )